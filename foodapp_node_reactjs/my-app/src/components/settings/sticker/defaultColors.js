import theme from '../themes/themesdefault'
const defaultColors = {
    labelColor: '#3C3C3C',
    statColor: '#900C3F',
    bgColor: '#ffffff',
    iconColor: '#ffffff',
    numberColor: '#900C3F',
    iconBgColor: [...theme.palette.widgets]
}

export default defaultColors;